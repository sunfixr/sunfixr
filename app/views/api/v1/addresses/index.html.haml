%h1 Listing addresses

%table
  %thead
    %tr
      %th Name
      %th Address1
      %th Address2
      %th City
      %th State
      %th Postal code
      %th Country
      %th Latitude
      %th Longitude
      %th
      %th
      %th
      
  %tbody
    - @addresses.each do |address|
      %tr
        %td= address.name
        %td= address.address1
        %td= address.address2
        %td= address.city
        %td= address.state
        %td= address.postal_code
        %td= address.country_id
        %td= address.latitude
        %td= address.longitude
        %td= link_to 'Show', api_v1_address_path(address)
        %td= link_to 'Edit', edit_api_v1_address_path(address)
        %td= link_to 'Destroy', api_v1_address_path(address), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Address', new_api_v1_address_path
